# Dirtybase documentation

Documentation site for the [Dirtybase](https://github.com/shiftrightonce/dirtybase) framework, built with VitePress.

## Development

Install dependencies with pnpm, then start the local documentation server:

```sh
pnpm install
pnpm docs:dev
```

Build the site for production with:

```sh
pnpm docs:build
```
